# clothi/.npmrc
# Configuración global del monorepo

# Usar npm workspaces
workspaces=true

# Evitar crear package-lock.json en cada sub-paquete
package-lock=false

# Instalar peerDependencies automáticamente
legacy-peer-deps=true

# No ejecutar scripts post-install automáticamente (seguridad)
ignore-scripts=true

# Usar registry de npm (puedes cambiarlo si usas privado)
registry=https://registry.npmjs.org/

# Cache configuration
cache=${HOME}/.npm-cache
prefer-offline=true

# Logging level (silent, error, warn, notice, http, timing, info, verbose)
loglevel=notice

# Limitar versión de engine (seguridad)
engine-strict=true

# Para desarrollo - no guardar exact versions en package-lock
save-exact=false

# Colores en los logs
color=true

# Evitar warnings de funding
fund=false

# Para Docker/CI - no mostrar progress bar
progress=false