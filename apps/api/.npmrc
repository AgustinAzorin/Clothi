# clothi/apps/api/.npmrc
# Configuración específica del backend

# Herencia de la configuración raíz
@clothi:registry=https://registry.npmjs.org/

# Para sequelize y paquetes que necesitan compilación nativa
sequelize_build=production
node-pre-gyp_accessKeyV2=

# Para bcrypt (evita compilación en cada install)
bcrypt_build_from_source=false

# Para sharp (procesamiento de imágenes)
sharp_libvips_binary_host=https://github.com/lovell/sharp-libvips/releases/download

# Configuración de build para node-gyp
node_gyp=../.npm-global/bin/node-gyp

# Cache para builds nativos
build-from-source-cache=${HOME}/.npm/build-cache

# Para entornos de CI (Render, Railway, etc.)
# Estas variables se sobrescriben en producción
production=false
NODE_ENV=development