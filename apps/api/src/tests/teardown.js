// Teardown file for Jest tests
// Este archivo se ejecuta después de que todos los tests hayan finalizado

module.exports = async () => {
  // Cerrar conexiones a BD si es necesario
  // Limpiar recursos globales
  // Restaurar mocks globales
};
